export type GameTextsArray = {
    text: string[]
}

export type GameCreators = {
    name: string,
    role: string,
    link: string
}

export interface IGalleryPicture {
    name: string,
    src: string,
    caption:string
}